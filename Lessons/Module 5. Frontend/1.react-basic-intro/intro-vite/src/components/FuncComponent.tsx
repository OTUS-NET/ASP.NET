//react component using function

const FuncComponent = () => { // Functional component using arrow function
    return (
        <>
            <div>
                <h1>Привет, FuncComponent!</h1>
            </div>
        </>
    );
};

export default FuncComponent;// export the component
